'use strict';

window.sr = ScrollReveal();


sr.reveal('.header-text, .text2-header', {
    reset: true,
    delay: 100
});
sr.reveal('p, span, i, .fab, .text-list', {
    reset: true,
    delay: 300
});
